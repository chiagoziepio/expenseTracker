const transactionReducer = (transaction ,action)=>{
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
    
}

export default transactionReducer